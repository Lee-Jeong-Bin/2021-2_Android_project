<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidproject.texipool">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Texipool">
        <activity
            android:name=".Reservationgroup"
            android:exported="false" />
        <activity
            android:name=".Participategroup"
            android:exported="false" />
        <activity android:name=".Finishmap" />
        <activity android:name=".Startmap" />
        <activity android:name=".Mypage" />
        <activity android:name=".PostUI" />
        <activity android:name=".Chat" />
        <activity android:name=".ListUI" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>